dist: trusty
language: cpp
sudo: required

addons:
  apt:
    sources:
      - george-edison55-precise-backports
    packages:
      - cmake-data
      - cmake

script:
 - mkdir build
 - mkdir bin
 - mkdir lib
 - cd build
 - rm -rf *
 - cmake ..
 - sudo make
 - ../bin/testlog
 
