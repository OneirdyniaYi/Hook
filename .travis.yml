dist: trusty
language: cpp
sudo: required

addons:
  apt:
    sources:
      - george-edison55-precise-backports
    packages:
      - cmake-data
      - cmake

script:
 - ls -ll
 - chmod a+x ..
 - mkdir build
 - cd build
 - cmake ..   
 - make
